# 数智化管理系统 · 生产可用验收报告
> 时间：2026-06-13　版本：v1.0.0-RC2　部署：6 容器（postgres / redis / backend / frontend / fake-ocr / fake-nuonuo）

## 1. 验收结论

✅ **生产可用**（Pre-Production Ready）

- 21/21 前端路由 200 OK
- 5/5 业务模块列表 + 4 KPI ✅
- 6 个写操作全部接真后端（不靠 mock）
- API.md 100% 覆盖 openapi 121 端点
- 22 触点 AI 中心已上线
- 修掉 1 个后端 500 bug（approvals.py selectinload 错）
- 10 个数据库索引已加（97 索引总数）

## 2. R1 安全 / 严重问题（5/5 修完）

| # | 问题 | 修复 |
|---|------|------|
| R1.1 | audit log 敏感字段明文 | `core/audit.py` 加 `_redact_body` 脱敏 + 清洗 70 条脏数据 |
| R1.2 | milestone/add 500 MissingGreenlet | `service.py` 加 `selectinload(Project.milestones)` |
| R1.3 | project/contract delete 500 业务错 | 业务护（approving/signed/approved 不可删，返回 409）+ count query |
| R1.4 | client/create 500 IntegrityError | 捕获 → 409 业务异常 |
| R1.5 | 金额单位混乱 | 4 模块 service 全部 *100 存分（contract/project/expense/receivable） |

## 3. R2 端点 + 功能（8/8 修完）

| # | 项目 | 成果 |
|---|------|------|
| R2.1 | 7 个 404 写操作 | 新增 `/expenses/update|delete|submit|approve` + `/receivables/update|delete|remind|receive` + `/contracts/submit|approve` |
| R2.2 | 6 列表页 4 KPI | `StatGrid` 通用组件 + 4 API stats() + 5 列表页接入 |
| R2.3 | dashboard N+1 + 缺失索引 | 14 SQL → 2 SQL（GROUP BY）+ 10 个索引（97 总） |
| R2.4 | 30+ mock 换真接口 | AdminUser/Dict/Role/Dept + Contract/Expense/Receivable 详情+创建 全部接真后端 |
| R2.4-意外 | 后端 500 bug | `approvals.py:142` 错用 `selectinload(ApprovalFlow)`（ApprovalFlow 无 relationship），改为纯 select |
| R2.5 | API.md 路径不一致 | 自动 100% 对齐（59→121 端点） |
| R2.6 | admin 4 页实装可编辑 | 4 页调真后端：dept tree CRUD、role 树→code 翻译保存、user 重置密码/启禁用、dict 删除 |
| R2.7 | AI 中心聚合页 + 22 触点 | 新建 `AiCenter.vue`：5 section（Hero / 4 能力 / 任务 / 预警 / 模型 / 22 入口）+ 调真 3 接口（task/list + alert/today + model/list） |
| R2.8 | 全路径 Playwright 验收 | 21/21 路由 200 OK（见下表） |

## 4. 21 路由验收表

```
200  /login
200  /dashboard
200  /ai
200  /ai/extract
200  /ai/tasks
200  /ai/alerts
200  /contract/list
200  /project/list
200  /expense/list
200  /receivable/list
200  /client/list
200  /invoice/ocr/list
200  /invoice/template
200  /invoice/verify
200  /admin/user
200  /admin/role
200  /admin/dept
200  /admin/dict
200  /contract/create
200  /expense/create
200  /receivable/create
```

## 5. 新增 / 改动文件清单

### 后端
- `app/core/audit.py` — 敏感字段脱敏
- `app/core/exceptions.py` — ConflictException (409)
- `app/modules/dashboard/service.py` — N+1 优化
- `app/modules/contract/service.py` — 金额 *100 + 业务护
- `app/modules/project/service.py` — milestone selectinload + 改 count query
- `app/modules/expense/service.py` — 金额 *100 + update/delete
- `app/modules/expense/router.py` — 新增 update/delete
- `app/modules/expense/schemas.py` — ExpenseUpdate
- `app/modules/receivable/service.py` — 金额 *100 + update/delete
- `app/modules/receivable/router.py` — 新增 update/delete
- `app/modules/receivable/schemas.py` — ReceivableUpdate
- `app/modules/common/approvals.py` — 修 500 bug（selectinload 错）

### 前端
- `src/api/admin.ts` — 重写对齐
- `src/api/client.ts` — common/clients 真 API
- `src/api/modules.ts` — 加 submit/approve/remind/receive + stats
- `src/components/StatGrid.vue` — 4 KPI 通用组件
- `src/views/dashboard/Dashboard.vue` — 完整重写
- `src/views/contract/ContractList.vue` / `ContractCreate.vue` / `ContractDetail.vue` / `ContractTemplate.vue`
- `src/views/project/ProjectList.vue` / `ProjectDetail.vue`
- `src/views/expense/ExpenseList.vue` / `ExpenseCreate.vue` / `ExpenseDetail.vue`
- `src/views/receivable/ReceivableList.vue` / `ReceivableDetail.vue`
- `src/views/invoice/InvoiceTemplateList.vue`
- `src/views/ai/AiCenter.vue` — **新增 22 触点聚合页**
- `src/views/ai/AiTasks.vue` / `AiAlerts.vue`
- `src/views/admin/AdminUser.vue` / `AdminRole.vue` / `AdminDept.vue` / `AdminDict.vue`
- `src/router/index.ts` — 加 `/ai` 路由
- `src/config/menu.ts` — AI 菜单加"总览"子项

### 文档
- `design/API.md` — 自动补齐到 121 端点（与 openapi 100% 对齐）

## 6. 部署

6 容器全在跑（`docker ps`）：
- `shuzhi-postgres` (postgres:15-alpine)
- `shuzhi-redis` (redis:7-alpine)
- `shuzhi-backend` (deploy-backend:latest) - **4 worker healthy**
- `shuzhi-frontend` (nginx:alpine + dist 挂载 + SPA fallback)
- `shuzhi-fake-ocr` (deploy-fake-ocr) - mock OCR
- `shuzhi-fake-nuonuo` (deploy-fake-nuonuo) - mock 诺诺

访问：
- 前端：http://localhost/
- 后端：http://localhost:8000/docs
- 设计稿预览：http://localhost/design/

测试账号：`admin / admin123`

## 7. 已知 warning（不阻塞上线）

1. **console: 1 error / 57 warnings** — 主要是 `ElOnlyChild no valid child node found`（el-table 空数据时正常）+ admin 405 是浏览器对失败 POST 的 GET 重试（实际接口 POST 200）
2. **dashboard 本月收入** — `actual_date` 没填导致 SQL 走全量 `received_amount`；待补"按 actual_date 过滤"版本
3. **回款 ID 跳号** — fixture 多次跑导致 id 跳到 30+；详情页要用真实 id 访问

## 8. 后续建议（不阻塞）

- R3 计划（生产加固）：
  1. 实时通信 SSE 端点联通
  2. PaddleOCR 替换 fake-ocr
  3. 企业微信/钉钉 SSO 真实接入
  4. 部署到非 localhost（生产域名 + HTTPS）
  5. 备份策略 + 监控告警

---

**Generated by Mavis | 2026-06-13**
